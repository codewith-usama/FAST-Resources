#include<stdio.h>
#include<string.h>
//#include<strln.h>
main()
{
	char str[1000];
	//char c;
	int i, c, n, j, a;
	//printf("How many strings you are enternig: ");
	//scanf("%d", n);
	
	for(i=0;i<=0;i++)
	{
		printf("%d. Enter a string:", j);
		j++;
		scanf(" %s", &str[i]);
	}
	
	str.size()=a;
	if(str(a)>10)
    //	sizeof(str) = c[i];
	{
		printf("String length is %d", str[i]);
	}
}
